general:
  branches:
    ignore:
      - gh-pages
      - ssr

machine:
  node:
    version: 6

dependencies:
  pre:
    - npm i -g npm yarn
    - rm -rf .yarnrc yarn.lock
  override:
    - yarn

test:
  override:
    - yarn lint && yarn unit
