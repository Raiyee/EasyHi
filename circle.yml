general:
  branches:
    only:
      - master

machine:
  node:
    version: stable

dependencies:
  pre:
    - npm i -g npm yarn@rc
    - yarn cache clean
    - rm -rf .yarnrc yarn.lock node_modules
  override:
    - yarn

test:
  override:
    - yarn lint && yarn unit
