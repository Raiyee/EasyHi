general:
  branches:
    only:
      - master

machine:
  node:
    version: 6

dependencies:
  pre:
    - npm i -g npm yarn
    - rm -rf .yarnrc yarn.lock
  override:
    - yarn

test:
  override:
    - yarn lint && yarn unit
