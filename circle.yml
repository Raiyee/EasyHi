general:
  branches:
    only:
      - master

machine:
  node:
    version: stable

dependencies:
  pre:
    - npm i -g npm node-gyp yarn
    - rm -rf .yarnrc yarn.lock
  override:
    - yarn

test:
  override:
    - yarn lint && yarn unit
