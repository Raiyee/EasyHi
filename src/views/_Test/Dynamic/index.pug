div(:class="classes.container")
  .text-center
    button.btn(:class="classes.btn", @click="rebuild") {{ built ? "Clear" : "Rebuild" }} your own view
  hr
  dynamic(ref="component", :comps="comps", :emptyView="emptyView", @built="compBuilt")
