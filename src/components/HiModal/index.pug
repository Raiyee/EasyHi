div(v-if="modals.length")
  div(v-if="currModal && currModal.options.backdrop", :class="classes.modalBackdrop")
  component(v-for="{component, id, props, options} of modals",
  :is="component",
  :key="id",
  ref="modal",
  v-bind="props",
  v-show="options.show")
